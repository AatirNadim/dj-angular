import { TempDirDirective } from './temp-dir.directive';

describe('TempDirDirective', () => {
  it('should create an instance', () => {
    const directive = new TempDirDirective();
    expect(directive).toBeTruthy();
  });
});
